package-lock=false

# npm configuration for publishing
# This file is used by CI/CD to publish to npm

# Set the registry (default is public npm)
registry=https://registry.npmjs.org/

# Use provenance for package verification (required for npm provenance)
provenance=true
